import { useState } from 'react';
import { useNavigate, useLocation } from 'react-router-dom';
import { useAuth } from '../context/AuthContext';
export default function Auth(){ const isRegister = useLocation().pathname.includes('register'); const nav=useNavigate(); const {login,register}=useAuth(); const [form,setForm]=useState({name:'',email:'',password:'',role:'user'}); const submit=async e=>{e.preventDefault(); isRegister?await register(form):await login(form.email,form.password); nav('/dashboard')}; return <form onSubmit={submit} className="card max-w-md mx-auto mt-16 space-y-4"><h1 className="text-3xl font-bold">{isRegister?'Create account':'Login'}</h1>{isRegister&&<input className="input" placeholder="Name" onChange={e=>setForm({...form,name:e.target.value})}/>}<input className="input" placeholder="Email" onChange={e=>setForm({...form,email:e.target.value})}/><input className="input" type="password" placeholder="Password" onChange={e=>setForm({...form,password:e.target.value})}/>{isRegister&&<select className="input" onChange={e=>setForm({...form,role:e.target.value})}><option value="user">Candidate</option><option value="recruiter">Recruiter</option></select>}<button className="btn w-full">{isRegister?'Register':'Login'}</button></form> }
